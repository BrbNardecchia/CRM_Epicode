import { FatturefilterPipe } from './fatturefilter.pipe';

describe('FatturefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FatturefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
